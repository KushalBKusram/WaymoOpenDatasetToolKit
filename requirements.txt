# Waymo Open Dataset v2 (Parquet-based API)
# Pre-built wheels are Linux/Python 3.10 only.
# macOS: build from source — see https://github.com/waymo-research/waymo-open-dataset/blob/master/docs/quick_start.md
waymo-open-dataset-tf-2-12-0

# TensorFlow — used for tf.io.gfile GCS access and range-image decoding.
# On Apple Silicon install via: pip install tensorflow tensorflow-metal
tensorflow

# Parquet + GCS streaming
dask[dataframe]
gcsfs
google-cloud-storage
pandas
pyarrow

# Visualisation
opencv-python
open3d
numpy
