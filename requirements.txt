# Waymo Open Dataset v2 — Python dependencies
#
# The official `waymo-open-dataset-tf-*` package has Linux-only wheels and
# cannot be installed on macOS. We access v2 Parquet files directly with
# pandas/pyarrow/dask, so the waymo package is NOT required.
#
# TensorFlow is not required: GCS listing and LiDAR decoding use gcsfs and NumPy.

# Parquet + GCS streaming (no local download required)
dask[dataframe]
gcsfs
google-cloud-storage
pandas
pyarrow

# Visualisation
opencv-python
open3d
numpy
matplotlib
plotly

# Training + evaluation (optional — only needed for train.ipynb)
ultralytics
torchmetrics
pyyaml

# Notebook
jupyter
ipykernel

# Local explorer and evaluation dashboard
streamlit
