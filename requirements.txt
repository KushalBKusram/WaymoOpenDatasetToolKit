# Waymo Open Dataset v2 — Python dependencies
#
# The official `waymo-open-dataset-tf-*` package has Linux-only wheels and
# cannot be installed on macOS. We access v2 Parquet files directly with
# pandas/pyarrow/dask, so the waymo package is NOT required.
#
# TensorFlow IS still required for:
#   - tf.io.gfile.glob  → GCS file listing with ADC credentials
#   - tf.io.decode_raw  → range-image tensor decoding
# On Apple Silicon: `pip install tensorflow` auto-selects the Metal build.

tensorflow

# Parquet + GCS streaming (no local download required)
dask[dataframe]
gcsfs
google-cloud-storage
pandas
pyarrow

# Visualisation
opencv-python
open3d
numpy
matplotlib

# Training (optional — only needed for train.ipynb)
ultralytics

# Notebook
jupyter
ipykernel
